# Use npm registry
registry=https://registry.npmjs.org/
# Disable package-lock for pnpm
package-lock=true
# Use strict SSL
strict-ssl=true
# Increase network timeout for slow connections
network-timeout=60000
# Force legacy peer deps to avoid resolution issues
legacy-peer-deps=true
# Disable fund messages
fund=false
# Disable audit
audit=false
